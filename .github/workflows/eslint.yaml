name: ESLintint Tests

on: pull_request

jobs:
  jest:
    name: ESlint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "lts/iron"

      - run: npm ci

      - run: npm run lint:eslint:check
